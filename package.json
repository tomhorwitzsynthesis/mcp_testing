{
  "name": "supabase-mcp-on-render",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "mcp-proxy --server stream --streamEndpoint /mcp --port ${PORT:-10000} -- \"npx -y @supabase/mcp-server-supabase@latest --read-only --project-ref ${SUPABASE_PROJECT_REF}\""
  }
  "dependencies": {
    "mcp-proxy": "^5.5.6"
  }
}
