{
  "name": "supabase-mcp-on-render",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "mcp-proxy --server stream --streamEndpoint /mcp --port ${PORT:-10000} --shell \"npx -y @supabase/mcp-server-supabase@latest --read-only --project-ref ${SUPABASE_PROJECT_REF}\""
  },
  "dependencies": {
    "mcp-proxy": "^5.5.6"
  }
}
